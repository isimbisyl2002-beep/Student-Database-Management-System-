##1.	Create the database student_db. 
use student;

create table students(
student_id int not null,first_name varchar(50),last_name varchar(50),gender varchar(10),date_of_birth date,enrollment_year year,deprtment_id int);
desc students;

##4.	Insert the provided sample data into table. 
insert into students values(101,"alice","uwimana","female","2003-05-05",2023,1);
insert into students values(102,"jean","ndayishimiye","male","2002-11-08",2022,2);
insert into students values(103,"grace","mukamana","female","2004-02-20",2023,1);

##5.	Retrieve all students from a specific department. 
SELECT *
FROM Students
WHERE department_id = 1;

alter table students add constraint primary key(student_id);
desc students;

##2.	Create the Departments table. 
create table departments(department_id int not null,department_name varchar(50));
desc departments;

##3.	Create the Students table with appropriate primary and foreign keys. 
alter table departments add constraint primary key(department_id);
desc departments;

##4.	Insert the provided sample data into both tables. 
insert into departments values(1,"information technology");
insert into departments values(2,"business administration");
insert into departments values(3,"electrical engineering");
select * from departments;
select * from students;

desc students;

# Update the Department of a Student Using student_id
UPDATE Students
SET department_id = 2
WHERE student_id = 103;

##7.	Delete a student record using student_id. 
delete from  students where student_id=101;
select * from students;